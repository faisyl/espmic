// Package metrics models the statistics surface (spec §18).
//
// Tracks the counters listed in §18 (packet loss, jitter, bitrate, decode
// errors, etc.) for diagnostics and the stream-stats API.
package metrics
