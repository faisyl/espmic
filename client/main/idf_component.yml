## IDF Component Manager manifest
dependencies:
  idf:
    version: ">=5.0.0"
  # esp-tls is a bundled ESP-IDF component — it is NOT a registry component
  # and must not be declared here.
  78/esp-opus:
    version: "*"